import { StyleSheet, Text, View } from "react-native";

import React from "react";

const WordPlayGame = () => {
  return (
    <View>
      <Text>WordPlayGame</Text>
    </View>
  );
};

export default WordPlayGame;

const styles = StyleSheet.create({});
