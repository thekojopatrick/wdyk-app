import React from "react";
import { StyleSheet, Text, View } from "react-native";

const GameScoreScreen = () => {
  return (
    <View>
      <Text>GameScoreScreen</Text>
    </View>
  );
};

export default GameScoreScreen;

const styles = StyleSheet.create({});
